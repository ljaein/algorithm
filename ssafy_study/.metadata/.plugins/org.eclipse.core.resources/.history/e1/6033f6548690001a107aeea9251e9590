
public class test {

	public static void main(String[] args) {
		double disX = -30;
		double disY = -10;
		double angle = (Math.atan2(disX, disY) * 360) / Math.PI;
		System.out.println(angle);
	}

}
